import { MenuItem } from "./MenuItem";

export class MenuInputItem extends MenuItem {
    // private isEditing: boolean = false;
    // private _value: string;
    // private isEditable: boolean = true;
    //
    // constructor (name: string, private label: string, private defaultValue: string, private onAction?: (menuItem: MenuInputItem) => void) {
    //     super(name, label + ': ' + defaultValue);
    //
    //     this.defaultValue = defaultValue;
    //     this._value = defaultValue;
    // }
    //
    // action () {
    //     this.isEditing = !this.isEditing;
    //
    //     if (!this.isEditing && this.onAction) {
    //         this.onAction(this);
    //     }
    // }
    //
    // set value (value) {
    //     this._value = value;
    //     this.text = this.label + ': ' + value;
    // }
    //
    // get value () {
    //     return this._value;
    // }
}
